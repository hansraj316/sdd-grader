# Add Notification Digest

## Why

Users miss updates because every event sends a separate email; several have
disabled notifications entirely.

## What Changes

- Batch each day's notification emails into a single digest.
- Add a per-user digest opt-out that restores immediate emails.
